/**
 * Feed right rail — deliberately empty.
 *
 * This 320px column is reserved for sponsored content (INVESTOR_PLAN.md §6,
 * monetization lane "ads"): native feed placements, map sponsor pins,
 * schedule interstitials. No fake brands, no widgets — the blank space is
 * the honest ad slot. Previous content (mock VOLT ad, Local Scan MiniMap,
 * Messages shortcut) removed by decision, Aug 16, 2026.
 */
export const RightSidebar = () => <aside className="hidden lg:block" aria-hidden />;
